{
  "name": "service-status-api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A dependency-free REST API for services, incidents and operational summaries.",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/repository.js && node --check src/validation.js"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
